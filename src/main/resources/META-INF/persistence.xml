<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">

    <persistence-unit name="databaseproperties" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
    
        <class>com.Bank.Entity.BankUserDetails</class>
        <class>com.Bank.Entity.StatementDetails</class>
        <class>com.Bank.Entity.AdminDetails</class>

        <properties>
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver" />
            <property name="javax.persistence.jdbc.url" value="${DATABASE_URL}"/>
            <property name="javax.persistence.jdbc.user" value="${DATABASE_USERNAME}"/>
            <property name="javax.persistence.jdbc.password" value="${DATABASE_PASSWORD}"/>

            <!-- This property is for Hibernate's DDL generation (schema creation) -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- This property tells Hibernate to show the SQL in logs -->
            <property name="hibernate.show_sql" value="true"/>
            <!-- This property is for Hibernate's dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>
        </properties>
    </persistence-unit>
</persistence>